"use client";

import { useGlobalContext } from "@/Context/store";

export default function ProjectPDF() {
    const {
        isEditing,
        projectPDFUrl,
        setProjectPDFUrl,
        errors,
    } = useGlobalContext();

    return (
        <>
        </>
    )
}